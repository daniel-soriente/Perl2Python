#!/usr/bin/perl -w
# Demo for Subset2 - Operators

$num = "num";

if (!$num lt "njdakdu") {
	while((!$num ne "num") || (($num gt "num"))) {
		$num = "he";
		print "num changed\n";
		last;
	}
	if ($num eq "hello" && $num le "hello") {
		print "wow\n"
	}
}
